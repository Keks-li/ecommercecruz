1. **Apply the provided HTML layout to the React application.**
   - Update `tailwind.config.js` with the provided configuration.
   - Update `frontend/index.html` to match the given `head` and root wrapper.
   - Update `frontend/src/index.css` to add necessary utility classes like `.no-scrollbar`.
   - Update `frontend/src/components/shop/Navbar.jsx` with the header styling and icons.
   - Update `frontend/src/components/shop/Hero.jsx` with the provided Hero section markup and images.
   - Update `frontend/src/components/shop/CategoryBar.jsx` to match the new visual design.
   - Update `frontend/src/components/shop/PromoBanners.jsx` with the provided combo section markup and images.
   - Update `frontend/src/components/shop/ProductCard.jsx` to follow the product card design in the provided HTML.
   - Update `frontend/src/pages/ProductGallery.jsx` to glue the updated components, add a section title for the single products, and add the fixed bottom navbar for mobile.
   - Update `frontend/src/components/shop/Footer.jsx` with the provided footer markup.
   - Update `frontend/src/components/shop/CartDrawer.jsx` to adapt the classes to the new design token variables.

2. **Test and Verify the Application**
   - Check if the React application builds and renders successfully.
   - Verify visually that the React application matches the provided HTML format.

3. **Complete Pre-commit Steps**
   - Run `pre_commit_instructions` tool to verify the codebase against rules.

4. **Submit**
   - Submit the changes to complete the task.
